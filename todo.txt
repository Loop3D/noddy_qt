COMMENTS IN ALL CAPS. 


45) Don't do yet! When a petrophysical value is retrieved prior to building a density or susceptibilty volume for calculating an anomaly, add check box and editable parameters value to the geophsyics calc options that will add plurigaussian 3D geospatial noise to each voxels density and susceptiblity (ie all values will vary in a way to make a "patchy" petrophysics dataset). Each stratigraphic unit will have its own pattern that randomly varies the chosen upper and lower limits.

46) Don't do yet! When calculating triagulated surfaces for discontinuities (faults, dykes, unconformities, plugs), currently the code applies a fixed routine for discontinuities that results in an "egg carton" roughness. Local surfaces use a modified marching cube algoithm that splits a cube into 5 tetrahedra, and the codes at each vertex are used to decide what to do. Instead for tetrahedra that have vertices one side or other of the disctonitutiy, we can use the calcs to go back from the current xyz position and calculate how far the back in time point is from the discintinuity surface (with sign showing aboveor below the surface) and then we could fit a plane through the dist=0 values (just like we do for the stratigraphic surfaces) Maybe read this paper first to get an understanding of the logic of eulerian kinematics calcsc\ C:\Users\00073294\Dropbox\noddy\noddy_papers\jessell_valenta_1996.pdf I guess any tetrahedra that share a face with such modified calcs will also have to be modified somehow as the resulting traingles edges need to be coincident.

The code too look at for this change is in AlphaTet.c and beta, gamma, delta. 


90) On macos there are no menus ecept one called noddy